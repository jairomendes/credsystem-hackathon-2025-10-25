# OpenRouter configuration
# Copie este arquivo para ".env", preencha os valores sensíveis
# e carregue com `source .env` antes de subir a API.

# Chave da API do OpenRouter (obrigatória para chamadas reais)
OPENROUTER_API_KEY=coloque_sua_chave_aqui

# Opcional: altere se quiser apontar para outro endpoint
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# Opcional: ajuste o modelo conforme assinatura/necessidade
OPENROUTER_MODEL=openrouter/auto

# Opcional: substitua o prompt padrão do roteador (manter vazio usa o prompt embutido)
OPENROUTER_SYSTEM_PROMPT=

# Opcional: timeout em segundos para requisições à API do OpenRouter
OPENROUTER_TIMEOUT_SECS=30

# Opcional: cabeçalhos de atribuição solicitados pela OpenRouter
OPENROUTER_HTTP_REFERER=https://suaaplicacao.com
OPENROUTER_TITLE=Minha Aplicacao
